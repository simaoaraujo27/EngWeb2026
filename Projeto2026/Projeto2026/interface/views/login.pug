extends layout

block content
  // Modal de Conta Desativada
  if error && error.includes('desativada')
    div#disabled-account-modal(class="fixed inset-0 z-50 flex items-center justify-center p-4 bg-edu-dark/60 backdrop-blur-sm transition-opacity")
      div(class="bg-white dark:bg-slate-900 w-full max-w-sm rounded-3xl shadow-2xl overflow-hidden animate-in fade-in zoom-in duration-300")
        div(class="p-8 text-center")
          div(class="w-16 h-16 bg-red-50 dark:bg-red-900/20 rounded-2xl flex items-center justify-center mx-auto mb-6")
            i(data-lucide="user-x" class="text-red-500 w-8 h-8")
          h2(class="text-xl font-extrabold text-edu-dark dark:text-white mb-2") Conta Desativada
          p(class="text-sm text-edu-muted dark:text-slate-400 mb-8 leading-relaxed")
            | Lamentamos, mas o acesso à sua conta foi suspenso. Para reativar o seu perfil ou obter mais informações, por favor contacte a administração do sistema.
          div(class="flex flex-col gap-3")
            a(href="mailto:admin@eduportal.pt" class="w-full py-3 bg-primary text-white rounded-xl font-bold hover:bg-primary-dark transition-all flex items-center justify-center gap-2")
              i(data-lucide="mail" class="w-4 h-4")
              | Contactar Suporte
            button(onclick="document.getElementById('disabled-account-modal').style.display='none'" class="w-full py-3 bg-gray-50 dark:bg-slate-800 text-gray-700 dark:text-slate-300 rounded-xl font-bold hover:bg-gray-100 dark:hover:bg-slate-700 transition-all")
              | Fechar

  div(class="min-h-[calc(100vh-4rem)] flex items-center justify-center bg-gradient-to-br from-blue-50 to-gray-50 dark:from-slate-950 dark:to-slate-900 py-12 px-4 transition-colors")
    div(class="w-full max-w-md")
      div(class="text-center mb-8")
        div(class="inline-flex items-center justify-center w-16 h-16 bg-primary rounded-2xl mb-4 shadow-lg shadow-primary/20")
          i(data-lucide="lock" class="text-white w-8 h-8")
        h1(class="text-3xl font-extrabold text-edu-dark dark:text-white") Bem-vindo de volta
        p(class="text-edu-muted dark:text-slate-400 mt-2") Entre na sua conta para continuar

      div(class="bg-white dark:bg-slate-900 rounded-3xl border border-gray-100 dark:border-slate-800 shadow-xl overflow-hidden")
        div(class="p-8")
          if error
            div(class="mb-6 p-4 bg-red-50 dark:bg-red-900/20 border border-red-100 dark:border-red-800 text-red-600 dark:text-red-400 rounded-xl text-sm flex items-center gap-2")
              i(data-lucide="alert-circle" class="w-4 h-4")
              = error

          form(class="space-y-6" action="/login" method="POST")
            div(class="space-y-2")
              label(class="text-sm font-bold text-gray-700 dark:text-slate-300") Email / ID
              div(class="relative")
                i(data-lucide="mail" class="absolute left-4 top-1/2 -translate-y-1/2 w-4 h-4 text-gray-400")
                input(class="w-full pl-11 pr-4 py-3 bg-gray-50 dark:bg-slate-800 border border-gray-100 dark:border-slate-700 rounded-xl text-sm focus:outline-none focus:ring-2 focus:ring-primary/20 focus:border-primary dark:text-white transition-all" type="email" name="_id" placeholder="seu@email.com" required)

            div(class="space-y-2")
              div(class="flex items-center justify-between")
                label(class="text-sm font-bold text-gray-700 dark:text-slate-300") Password
                a(href="#" class="text-xs text-primary dark:text-blue-400 hover:underline") Esqueceu-se?
              div(class="relative")
                i(data-lucide="key" class="absolute left-4 top-1/2 -translate-y-1/2 w-4 h-4 text-gray-400")
                input(class="w-full pl-11 pr-4 py-3 bg-gray-50 dark:bg-slate-800 border border-gray-100 dark:border-slate-700 rounded-xl text-sm focus:outline-none focus:ring-2 focus:ring-primary/20 focus:border-primary dark:text-white transition-all" type="password" name="password" placeholder="••••••••" required)

            button(class="w-full py-4 bg-primary text-white rounded-xl font-bold hover:bg-primary-dark transition-all shadow-lg shadow-primary/20" type="submit")
              | Iniciar Sessão

          div(class="mt-8 pt-8 border-t border-gray-50 dark:border-slate-800 text-center")
            p(class="text-sm text-gray-500 dark:text-slate-400")
              | Não tem conta? 
              a(href="/register" class="text-primary dark:text-blue-400 font-bold hover:underline") Registe-se aqui

